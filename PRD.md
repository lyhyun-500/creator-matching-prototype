# 크리에이터 매칭 추천 프로토타입 PRD

버전: 1.1 · 작성일: 2026-09-11 · 최종 수정: 2026-09-12 · 상태: 1차 구현 반영

대상: 프로덕트 기획 사전과제 / Claude Code 구현 핸드오프

> v1.1 변경 사유: 1차 구현을 실제 화면에서 확인한 결과, 후보가 많을 때 스크롤 부담이 크고 특히 견적 확인 필요 후보가 기존 후보 목록 아래에 있어 접근하기 어렵다는 문제가 확인되었다. 이를 해결하기 위해 7.1~7.3절을 예산 입력 방식(금액 추가 버튼)과 결과 탐색 방식(탭·페이지네이션·카드 축약)에 맞춰 갱신했다. 추천 계산식(6절)과 필터 정책(4~5절)은 변경하지 않았다.

## 1. 제품 정의

광고주가 예산·카테고리·팔로워 규모를 입력하면, 조건에 맞는 크리에이터를 평균 조회수 중심으로 추천하고 참여율·협업 이력·비용 대비 조회 지표를 비교할 수 있는 웹 프로토타입이다.

핵심 사용자 가치는 **적합한 후보를 빠르게 추려 다음 협업 검토로 넘어가는 것**이다. 조회수 예측 모델이나 자동 계약 서비스가 아니다. 본 문서의 사업 구조는 제품 설계를 위한 가설이며, 특정 회사의 실제 수익 모델이나 내부 운영을 확인한 사실로 제시하지 않는다.

## 2. 문제와 사업 가설

### 2.1 사용자와 상황

주 사용자는 크리에이터 캠페인을 검토하는 광고주 마케팅 담당자다. 보조 사용자는 광고주에게 후보를 제안하는 대행사 담당자다. 사용자는 협업 예산과 카테고리, 원하는 크리에이터 규모를 알고 있지만 후보 데이터를 일일이 비교하는 데 시간이 든다.

주요 문제는 다음과 같다.

- 팔로워만으로는 실제 콘텐츠 소비 규모를 판단하기 어렵다.
- 조회수, 참여율, 평점, 가격의 단위가 달라 우선순위를 정하기 어렵다.
- 캠페인 이력이 없는 후보를 무료 또는 저평가 후보로 오해할 수 있다.
- 조건에 맞는 후보가 없으면 어느 조건을 바꿔야 하는지 알기 어렵다.

### 2.2 사업 가치가 발생하는 경로

후보 탐색·비교 시간 감소 → 후보 선정과 견적 검토 진입 개선 → 장기적으로 계약 전환 및 담당자당 처리 캠페인 수 개선을 기대한다. 거래 중개라면 성사 수수료, 운영 대행이라면 대행료와 운영 효율에 연결될 수 있다. 이번 프로토타입은 탐색·비교까지 구현하며 견적 요청·계약·결제 및 실제 매출 효과는 구현하거나 입증하지 않는다.

### 2.3 검증 지표

| 단계 | 지표 정의 | 검증 방법 |
| --- | --- | --- |
| 프로토타입 | 조건 입력 시작부터 적합한 후보 3명을 선정할 때까지의 시간 | 동일 과업으로 원본 표를 보는 방식과 프로토타입 사용을 비교. 후보 부족 시 별도 과업 사용 |
| 프로토타입 | 추천 근거 이해 여부 | 사용자가 추천 이유와 과거 평균 집행액의 한계를 설명할 수 있는지 확인 |
| 프로토타입 | 빈 결과 회복률 | 0건 검색 중 제안 조건 적용 후 이력 보유 후보가 1명 이상 나온 검색 비율 |
| 상용화 이후 | 견적 요청률 | 후보 결과를 본 검색 세션 중 견적 요청이 발생한 세션 비율 |
| 상용화 이후 | 계약 전환율 | 유효 견적 요청 중 계약이 성사된 비율 |
| 상용화 이후 | 운영 효율 | 담당자 1인당 처리 캠페인 수와 캠페인별 투입 시간 |

현재 기준값과 목표 개선율은 미측정이다. 임의의 개선 수치를 성과로 기재하지 않는다. 분석 SDK 연결은 MVP에서 제외하고 수동 사용성 검증을 우선한다.

## 3. 목표와 범위

### 3.1 MVP에 포함

1. 예산 숫자 입력, 카테고리 다중 선택, 팔로워 규모 단일 선택.
2. CSV 원본 보존, 안전한 파싱 및 런타임 전처리.
3. 조건 필터와 도달 중심 가중치 추천.
4. 후보 카드, 추천 근거 및 점수 상세.
5. 추천순·조회수순·참여율순·협업점수순·비용 대비 조회 지표순 정렬.
6. 이력 없는 후보의 별도 노출.
7. 결과 0건에서 실제 데이터 기반 조건 완화 제안.
8. 로딩·입력 오류·데이터 오류·결과 상태 처리.

### 3.2 제외 기능과 이유

| 제외 항목 | 이유 |
| --- | --- |
| 여러 크리에이터의 예산 조합 최적화 | 이번 입력은 1명당 1회 협업 예산이며, 조합·중복 오디언스 문제는 범위 밖 |
| 실제 견적 요청·계약·결제·메시지 발송 | 프로토타입 데이터로 실제 거래를 실행할 수 없음 |
| 구매 전환·매출·ROAS 예측 | 전환, 매출, 광고 콘텐츠 성과 데이터 없음 |
| 로그인·DB·관리자·실시간 크롤링 | 200행 고정 데이터의 추천 검증에 불필요 |
| 플랫폼 필터와 플랫폼별 보정 모델 | 필수 입력에 집중. 플랫폼 정보는 표시하되 추가 필터와 검증되지 않은 보정은 제외 |
| 성별·연령·지역·브랜드 안전성 필터 | 필요한 데이터 없음 |
| 이용자가 가중치를 조정하는 UI | 초기 제품의 도달 우선 목적을 일관되게 검증하기 위해 제외 |
| 신규 후보 가격 추정 | 가격 학습에 필요한 계약 조건과 충분한 근거 없음 |
| 즐겨찾기·내보내기·다중 캠페인 관리 | 후속 업무 기능으로 분리 |

## 4. 핵심 정책과 설계 결정

| 결정 | 채택 정책 | 근거 및 트레이드오프 |
| --- | --- | --- |
| 추천 목적 | 도달 > 반응 > 협업 이력 | 사용자가 선택한 우선순위. 가중합이므로 조회수 내림차순과 동일하지 않음 |
| 예산 의미 | 크리에이터 1명과 1회 협업할 때의 예산 상한, KRW | 전체 캠페인 배분 문제를 분리 |
| 가격 비교 | 과거 평균 집행액 ≤ 예산 | 누적 집행액은 1회 협업 가격을 대표하지 않음. 평균액도 현재 견적은 아님 |
| 카테고리 | 선택 항목 간 OR | 후보가 선택한 카테고리 중 하나에 속하면 통과 |
| 팔로워 | 규모 필터에만 반영 | 조회수와 중복 가점하지 않음 |
| 비용 효율 | 참고 지표 표시 및 별도 정렬 | 기본 점수에 추가하면 도달과 가격의 중복 반영 및 저가 후보 편향 가능 |
| 신규 후보 | 견적 확인 필요 영역으로 분리 | 가격 미확인을 예산 적합 또는 부적합으로 단정하지 않음 |
| 정규화 기준 | 같은 규모의 유효 데이터 전체 | 검색 조건 변경에 따라 같은 후보의 점수가 바뀌지 않도록 함 |

비용 대비 조회 지표를 기본 점수에 넣지 않는 것은 의도적이다. 광고주의 도달 우선 목적은 유지하고, 비용 효율을 중시하는 사용자가 별도 정렬로 판단하게 한다.

## 5. 데이터 계약 및 전처리

### 5.1 입력

제공 파일: `dummy_creators.csv`, 200행, 11열. UTF-8 BOM이 포함되어 있으므로 파서에서 BOM을 제거한 헤더를 사용한다. CRLF와 인용부호를 지원하는 CSV 파서를 사용하고 단순 쉼표 분할은 사용하지 않는다. 원본 파일의 바이트를 수정하지 않고 별도 메모리 객체로 변환한다.

| 필드 | 해석 및 사용 |
| --- | --- |
| creator_id | 고유 식별자, 동점 정렬 기준 |
| creator_name | 표시 이름 |
| category | 카테고리 OR 필터 |
| platform | 카드 표시. 플랫폼 간 지표 정의가 같다고 보장하지 않음 |
| followers | 0 이상의 정수, 규모 구간 계산 |
| avg_view_count | 0 이상의 정수, 도달 대리 지표 |
| engagement_rate | 0~100 숫자, 퍼센트 단위. 7.7을 7.7%로 표시 |
| total_campaign_count | 0 이상의 정수, 협업 경험 |
| total_campaign_budget_krw | 0 이상의 정수, 누적 이력 확인. 점수와 예산 필터에는 미사용 |
| avg_campaign_budget_krw | 0 이상의 정수, 과거 평균 집행액 |
| advertiser_rating | 캠페인 이력이 없으면 공란(null). 값이 있으면 제공 데이터 명세에 따른 0~5 숫자(5점 만점) |

숫자의 허용 범위는 프로토타입의 데이터 계약이다. 평점 척도는 제공 데이터 명세에 따른 5점 만점이다.

### 5.2 안전 처리

- 문자열 앞뒤 공백 제거 후 변환한다. 빈 문자열을 숫자 0으로 자동 변환하지 않는다.
- 필수 헤더 누락, CSV 파싱 실패, 중복 creator_id는 파일 수준 오류로 처리한다. 추천을 중단하고 오류 상태를 표시한다.
- 식별자·이름·카테고리·플랫폼 누락 또는 필수 숫자의 결측·NaN·Infinity·음수·범위 위반은 해당 행 제외 사유로 기록한다.
- 캠페인 수가 0이고 평점 null, 평균·누적 집행액 0이면 정상 신규 후보다. 오류 행으로 취급하지 않는다.
- 캠페인 수가 0인데 평점 또는 양의 집행액이 존재하거나, 캠페인 수가 양수인데 평균·누적 집행액이 0이면 이력 불일치로 해당 행을 제외한다.
- 캠페인 수가 양수이고 가격은 유효하지만 평점만 null이면 후보를 유지한다. 협업 계산에서 평점 부분만 50점으로 대체하고 ‘평점 미확인’ 표시. 실제 평점처럼 표기하지 않는다. 이 경우는 현재 제공 데이터에는 없는 방어 정책이다.
- 평균 집행액을 누적액/횟수로 덮어쓰지 않는다. 기간·집계 방식이 불명확하며 제공 평균값을 사용한다.
- 일부 행이 제외되면 ‘일부 데이터 N건을 제외했습니다’를 표시하고 개발 로그에 ID와 원인을 남긴다. 유효 행 0건이면 데이터 오류 상태로 처리한다.
- 현재 제공 데이터의 정상 신규 후보는 27명이다. 이 27명을 오류 또는 무료 후보로 분류하면 실패다.

### 5.3 규모 구간

| 규모 | 범위 | 제공 CSV 기준 인원 |
| --- | --- | ---: |
| 나노 | 0 ≤ followers < 10,000 | 44 |
| 마이크로 | 10,000 ≤ followers < 100,000 | 129 |
| 매크로 | followers ≥ 100,000 | 27 |

이 구간은 프로토타입에서 정한 명시적 기준이며 업계의 유일한 표준이라고 주장하지 않는다. 새로운 데이터에서는 경계값을 동일하게 적용한다.

## 6. 추천 계산 명세

### 6.1 처리 순서

CSV 로드 → 파싱·검증 → 유효 데이터 및 규모별 기준 집합 생성 → 입력 검증 → 카테고리·규모 필터 → 이력 보유/신규 분리 → 이력 보유 후보에 예산 필터 → 점수 및 지표 계산 → 정렬 → 결과·추천 근거·조건 완화 출력.

규모별 기준 집합에는 신규 후보도 포함한다. 신규 여부는 조회수·참여율의 유효성을 부정하지 않는다. 예산 초과 후보도 기준 집합에는 포함하며, 결과 후보에서만 제외한다.

### 6.2 조회수·참여율 정규화

각 지표를 동일 규모 내 백분위 0~100점으로 변환한다. 유효 기준 집합의 크기를 N, 해당 값보다 작은 값의 개수를 L, 동일한 값의 개수를 E라고 하면:

```text
percentile = 100 × (L + (E - 1) / 2) / (N - 1)
N = 1인 경우 percentile = 50
V = 평균 조회수의 percentile
E_score = 참여율의 percentile
```

동점은 평균 순위를 적용한다. 모든 값이 같으면 50점이다. 기준 집합은 CSV 로드 시 고정되고 예산·카테고리·정렬 변경으로 다시 구성하지 않는다.

백분위는 큰 조회수의 절대 규모가 다른 지표를 압도하는 문제를 줄이지만 실제 조회수 배수 차이를 보존하지 않는다. 따라서 원본 조회수와 조회수순 정렬도 제공한다. 점수는 동일 규모 내 상대 비교용이며 나노 80점과 매크로 80점의 도달량이 같다는 뜻이 아니다.

### 6.3 협업 점수

```text
R = advertiser_rating / 5 × 100
평점 미확인 이력 보유 후보에 한해 R = 50
M = 전체 유효 데이터의 최대 total_campaign_count
H = M > 0이면 100 × ln(1 + total_campaign_count) / ln(1 + M), 아니면 0
C = 0.70 × R + 0.30 × H
```

제공 데이터의 M은 30이다. 자연로그 밑은 분자·분모가 같으면 결과가 같다. 집행 경험은 증가할수록 추가 점수 효과가 줄어들게 한다. 70:30은 만족도를 경험보다 우선한 초기 가설이다. 캠페인 수는 리뷰 수가 아니므로 평점의 통계적 신뢰구간이나 베이지안 보정으로 설명하지 않는다. 협업 점수 역시 계약 성공 확률이 아니다.

### 6.4 최종 추천 점수

```text
S = 0.60 × V + 0.25 × E_score + 0.15 × C
```

60:25:15는 도달 우선 목적을 구현한 초기 정책이며 학습된 최적 가중치가 아니다. 기본 추천은 S 내림차순이다. 점수는 내부에서 반올림하지 않고 표시할 때만 소수 둘째 자리까지 반올림한다.

기본 정렬 동점 해소: S 내림차순 → 평균 조회수 내림차순 → 평균 집행액 오름차순 → creator_id 사전순 오름차순. 다른 정렬은 선택한 지표를 1순위로 적용하고 이후 기본 정렬 기준으로 해소한다.

### 6.5 비용 대비 조회 지표: 보완 사항

```text
참고 1천 조회당 비용 = avg_campaign_budget_krw / avg_view_count × 1,000
```

평균 집행액과 평균 조회수가 모두 양수인 이력 보유 후보에만 계산한다. 낮을수록 비용 대비 조회 관점에서 유리한 참고값이다. 0 조회수, 신규 후보, 유효 가격 없음은 null이며 ‘계산 불가’로 표시하고 정렬 시 마지막에 둔다. Infinity나 0원으로 표시하지 않는다.

표시 이름은 **‘참고 1천 조회당 비용’**으로 통일한다. 설명 문구: ‘과거 평균 집행액을 평균 조회수로 나눈 참고값입니다. 광고 콘텐츠의 실제 CPM이나 보장 성과가 아닙니다.’

평균액과 조회수가 동일 캠페인에서 측정된 쌍인지 확인할 수 없고 순도달 데이터도 없으므로 실제 광고 CPM, 예상 CPA, ROAS로 명명하지 않는다. 기본 추천 점수에는 넣지 않는다.

### 6.6 신규 후보

카테고리와 규모만 일치하는 신규 후보를 ‘견적 확인 필요’ 영역에 별도 노출한다. 예산 내 추천 건수에 포함하지 않는다.

```text
신규 내부 정렬 점수 = (0.60 × V + 0.25 × E_score) / 0.85
```

신규 영역은 이 내부 점수 내림차순 → 조회수 내림차순 → creator_id 오름차순으로 고정한다. 기존 후보의 정렬 컨트롤은 이 영역에 적용하지 않는다. 신규 점수를 기존 후보의 종합점수와 비교 가능한 숫자로 표시하지 않는다. 조회수·참여율과 각 백분위 근거만 보여준다.

표시: ‘캠페인 이력 없음’, ‘가격 미확인’, ‘견적 확인 필요’. 과거 평균액 0원을 노출하지 않고 평점은 ‘평가 없음’으로 표시한다. 신규 후보가 있다는 이유로 이력 보유 후보 0건 상태와 조건 완화 안내를 숨기지 않는다.

## 7. 화면과 상호작용

### 7.1 입력

- 예산: 원 단위 양의 안전 정수(Number.isSafeInteger)만 허용. 필수이며 0·음수·소수·공란·숫자가 아닌 값은 필드 오류. 입력 필드는 한 줄 금액 입력이며 천 단위 쉼표와 ‘원’ 단위를 표시에 보여주고, 검증 시에는 쉼표를 제거한 값을 사용한다.
  - 직접 입력에 더해 ‘+10만 원’, ‘+100만 원’, ‘+1,000만 원’ 금액 추가 버튼과 ‘금액 지우기’ 버튼을 제공한다. 추가 버튼은 현재 입력 금액에 해당 금액을 더하며, 1,000만 원은 증가 단위일 뿐 입력 상한이 아니다.
  - 공란 상태에서 추가 버튼을 누르면 0원을 기준으로 더한다. 저장된 값이 유효하지 않아 계산할 수 없는 경우(예: 안전 정수 범위 초과) 값을 임의로 바꾸지 않고 입력 오류를 안내한다.
  - 금액 추가·지우기 버튼은 그 자체로 검색을 실행하지 않는다. 검색은 ‘추천 보기’ 클릭으로만 실행되며, 기존 결과가 있는 상태에서 금액을 바꾸면 7.1의 ‘조건이 변경되었습니다’ 안내를 그대로 따른다.
- 카테고리: 유효 데이터에서 추출한 카테고리 목록, 다중 선택, 1개 이상 필수. 미선택을 전체 선택으로 취급하지 않는다.
- 규모: 나노·마이크로·매크로 중 1개 필수. 구간을 라벨 또는 도움말에 표시한다.
- 초기에는 필드 미입력과 시작 안내를 표시한다. 자동 추천하지 않는다.
- ‘추천 보기’로 검색한다. 결과는 마지막 제출 조건에 연결한다. 입력만 변경하면 ‘조건이 변경되었습니다. 다시 추천해 주세요’를 표시하고 이전 결과의 제출 조건을 유지한다.
- 초기화 시 입력·오류·결과·정렬·탭·페이지를 초기 상태로 되돌린다.

예산 도움말: ‘크리에이터 1명과 1회 협업하는 예산입니다. 과거 평균 집행액을 기준으로 후보를 찾으며 실제 견적은 달라질 수 있습니다.’

### 7.2 결과

결과는 ‘예산 기준 후보 (N명)’과 ‘견적 확인 필요 (M명)’ 두 탭으로 구분한다. 기본 활성 탭은 예산 기준 후보이며, 두 탭의 건수는 탭 레이블에 항상 표시해 신규 후보 존재 여부를 스크롤 없이 확인할 수 있게 한다. 신규 후보를 포함한 단일 숫자를 예산 적합 건수로 표현하지 않는다. 탭 바로 아래에는 ‘예산 기준 후보는 과거 평균 집행액을 기준으로 하며 실제 견적은 달라질 수 있습니다’를 상시 표시한다.

예산 기준 후보가 0명이어도 탭 자체는 유지하고 8절의 조건 완화 안내를 그대로 표시한다. 이때 견적 확인 필요 후보가 1명 이상이면 ‘견적 확인 필요 후보 N명 보기’ 버튼을 함께 표시해 클릭 시 견적 확인 탭으로 바로 이동시킨다.

카드 필수 표시(두 탭 공통 항목): 이름·ID, 플랫폼·카테고리, 팔로워·규모, 평균 조회수, 참여율. 예산 기준 후보 카드는 추가로 평점·집행 건수, 과거 평균 집행액, 참고 1천 조회당 비용, 종합점수를 한 줄 지표 목록으로 표시한다. 견적 확인 필요 카드는 평점 대신 ‘평가 없음’, 가격 대신 ‘가격 미확인’을 표시하고 종합점수에 해당하는 숫자는 노출하지 않는다.

추천 근거는 기본적으로 핵심 한 줄(도달 백분위 기준)만 표시한다. 예산 비교(‘과거 평균 집행액 780,000원 ≤ 입력 예산 2,000,000원’)와 평점·집행 이력 근거, 그리고 V/E_score/C 가중 기여점은 카드 자체 지표 목록에 이미 표시되지 않는 한 ‘추천 근거 상세 보기’를 펼쳐야 볼 수 있다. 근거를 삭제하지 않고 펼침 구조로 재배치한 것이며, 조회수가 1순위 지표라고 해서 모든 후보에 ‘조회수 우수’를 무조건 붙이지 않는다.

점수 설명: ‘같은 팔로워 규모 내 상대 지표를 조합한 점수입니다. 광고 성과를 보장하지 않습니다.’ ‘확정 견적’, ‘성과 보장’, ‘예상 매출’ 문구는 사용하지 않는다.

### 7.3 정렬과 페이지네이션

| 옵션 | 방향 |
| --- | --- |
| 추천순 — 기본 | 종합점수 내림차순 |
| 평균 조회수 높은 순 | 원본 평균 조회수 내림차순 |
| 참여율 높은 순 | 원본 참여율 내림차순 |
| 협업점수 높은 순 | 협업 점수 C 내림차순 |
| 참고 1천 조회당 비용 낮은 순 | 참고 비용 오름차순, null 마지막 |

정렬 컨트롤은 예산 기준 후보 탭 안에만 배치한다. 견적 확인 필요 탭은 6.6의 고정 내부 정렬(내부 점수 내림차순 → 조회수 내림차순 → ID 오름차순)을 유지하며 정렬 옵션을 노출하지 않는다. 정렬은 이력 보유 결과의 순서만 변경하며 필터·건수·점수를 변경하지 않는다.

각 탭은 전체 후보를 정렬한 뒤 10명 단위로 페이지네이션한다. 새로운 검색, 조건 완화 적용, 정렬 변경 시 해당 탭은 1페이지로 돌아간다. 탭 전환 자체는 상대 탭의 페이지 위치를 초기화하지 않는다. 페이지를 이동해도 후보가 중복되거나 누락되지 않아야 한다.

### 7.4 화면 상태

초기 안내 / 데이터 로딩 / 입력 오류 / 정상 결과(탭 2종) / 예산 기준 후보 0명+견적 확인 후보 존재 / 전체 후보 0명 / 일부 데이터 제외 경고 / 파일 로드·검증 오류를 구분한다. 파일 오류를 ‘조건에 맞는 후보 없음’으로 표시하지 않는다. 로드 실패는 재시도 버튼을 제공한다. 입력에는 레이블을 연결하고 키보드로 탭 전환·페이지 이동·선택·검색·정렬을 할 수 있어야 한다.

## 8. 빈 결과와 조건 완화

이력 보유 예산 적합 후보가 0명이면 아래 두 대안을 각각 계산한다. 신규 후보는 대안의 예산 적합 건수에서 제외한다. 모든 계산은 유효 데이터에 동일한 필터 함수를 적용한다.

1. **예산만 변경:** 현재 카테고리·규모의 이력 보유 후보 중 최소 평균 집행액 B_min을 찾는다. 후보가 있고 B_min > 현재 예산이면 ‘예산 B_min원으로 변경하면 N명’을 표시한다. N은 해당 예산 이하 후보 수다. 값과 버튼 적용값은 원 단위 정수로 일치해야 한다.
2. **규모만 변경:** 현재 카테고리와 예산을 유지하면서 다른 두 규모를 각각 계산한다. 1명 이상 나오는 규모만 ‘마이크로로 변경하면 N명’처럼 표시한다. 기존 선택을 대체하는 동작이며 여러 규모를 합치지 않는다.

카테고리는 브랜드 관련성을 훼손할 수 있어 자동으로 확장하지 않는다. 두 조건을 동시에 바꾸는 복합 제안도 MVP에서는 제외한다. 가능한 대안이 없으면 ‘예산 또는 카테고리·규모를 다시 설정해 주세요’와 조건 수정 동선을 제공한다.

대안은 클릭 전까지 입력과 결과에 영향을 주지 않는다. 클릭하면 해당 필드만 변경하고 자동 재검색하며 정렬은 추천순으로 초기화한다. 검색 결과의 건수와 제안 건수는 일치해야 한다.

## 9. 확인한 데이터 사례와 구현 수용 기준

아래 수치는 제공 CSV에 앞서 정의한 기준을 적용한 검증값이다. 데이터가 교체되면 재검증한다. 일부 시나리오의 검증이며 추천 품질 전반이 입증되었다는 뜻은 아니다.

### 9.1 정상 추천

뷰티·패션 / 마이크로 / 2,000,000원 → 기존 후보 21명, 신규 후보 3명.

| 순위 | ID | 평균 조회수 | 참여율 | 과거 평균 집행액 | 점수(소수 둘째 자리) |
| --- | --- | ---: | ---: | ---: | ---: |
| 1 | C0077 | 35,172 | 6.5% | 780,000원 | 78.19 |
| 2 | C0175 | 15,074 | 7.0% | 1,850,000원 | 67.59 |
| 3 | C0066 | 10,003 | 8.1% | 1,460,000원 | 67.24 |
| 4 | C0013 | 11,026 | 7.1% | 1,630,000원 | 62.95 |
| 5 | C0155 | 14,453 | 6.1% | 1,350,000원 | 62.03 |

식품 / 나노 / 500,000원 → 기존 후보 2명, 신규 후보 2명. 추천순은 C0091 → C0166이며 점수는 79.45, 45.79다.

### 9.2 가중치 민감도 확인

뷰티·패션 / 마이크로 / 200만 원에서 70:20:10은 상위 3명 유지 및 4·5위 교체, 50:30:20은 2·3위 교체 및 상위 5명 중 1명 교체였다. 첫 구현은 60:25:15를 사용한다. 이후 대표 조건별로 순위 적합성을 검토하며 가중치를 변경할 경우 이유와 결과를 기록한다. 민감도 옵션은 사용자 화면에 넣지 않는다.

### 9.3 주요 수용 기준

| ID | 시나리오 | 통과 기준 |
| --- | --- | --- |
| AC01 | 원본 파싱 | BOM 헤더 정상 처리, 원본 200행, 정상 신규 27명 보존 |
| AC02 | 규모 경계 | 9,999 나노 / 10,000 마이크로 / 99,999 마이크로 / 100,000 매크로 |
| AC03 | 예산 경계 | 과거 평균액과 동일 예산은 포함, 1원 낮으면 제외 |
| AC04 | 다중 카테고리 | OR로 포함하고 후보 중복 없음 |
| AC05 | 기준 집합 고정 | 예산·카테고리 변경 전후 동일 후보 점수 유지 |
| AC06 | 신규 처리 | null 평점이 0점으로 표시되지 않음, 0원이 무료/예산 적합으로 처리되지 않음 |
| AC07 | 정상 추천 | 9.1의 건수와 상위 순위 일치, 내부 점수 오차 0.01 미만 |
| AC08 | 빈 결과 | 피트니스 / 매크로 / 500,000원은 기존·신규 모두 0명 |
| AC09 | 대안 정확성 | AC08의 예산 제안 5,930,000원. 마이크로 500,000원 제안은 0명이므로 숨김. 다른 규모는 계산 결과에 따름 |
| AC10 | 대안 적용 | 클릭 전 조건 유지, 클릭 후 제안 필드만 변경, 표시 건수와 실제 결과 일치 |
| AC11 | 참고 비용 | C0077은 780000/35172×1000 ≈ 22,177원. 신규·조회수 0은 계산 불가 |
| AC12 | 정렬 | 각 옵션 방향 정확, 후보 집합과 개별 점수 불변, 동점 결과 결정적 |
| AC13 | 나눗셈 예외 | 기준 집합 1명·동일 값·최대 집행 건수 0에서도 NaN/Infinity 없음 |
| AC14 | 오류 구분 | 잘못된 입력, 파일 실패, 후보 0명 상태가 서로 다르게 표시 |
| AC15 | 불완전 이력 | 이력 보유+평점 null은 중립 대체와 미확인 표시, 이력 불일치는 행 제외 |

원본에 없는 경계·오류 케이스는 별도 테스트 fixture로 만든다. 테스트를 위해 제공 CSV를 수정하지 않는다.

## 10. 한계와 후속 검증

- 평균 조회수는 순도달 인원이나 광고 콘텐츠 예상 조회수가 아니다. 조회 기간·콘텐츠 유형·플랫폼 정의가 제공되지 않았다.
- 참여율의 분모가 불명확하므로 조회수×참여율을 예상 반응수로 만들지 않는다.
- 플랫폼별 지표 정의 차이는 현 버전에서 보정하지 않는다. 플랫폼을 카드에 표시하고 동일 플랫폼끼리도 사용자가 비교할 수 있게 한다.
- 평점과 다른 변수의 낮은 상관관계를 통계적 독립이나 성과 예측력의 증거로 주장하지 않는다.
- 과거 평균 집행액에는 콘텐츠 형식·제작 범위·사용권·세금 등의 조건이 불명확하다. 실제 예산 적합성은 견적 단계에서 재확인이 필요하다.
- 백분위와 가중치는 탐색 순서를 만드는 규칙이다. 실제 광고주 선택 및 집행 성과와의 관계는 미검증이다.
- 신규 후보의 별도 영역은 오해를 줄이지만 노출이 줄 수 있다. 기존 결과가 없어도 신규 영역을 숨기지 않으며, 상용화 시 신규 후보의 조회·문의 비율을 관찰한다.
- 비용 효율 정렬은 저가 후보를 선호할 수 있다. 타깃 일치·콘텐츠 품질·브랜드 안전성 데이터가 추가되기 전 자동 계약 결정에 사용하지 않는다.

## 11. Claude Code 구현 핸드오프

### 11.1 실행 원칙

이 PRD를 구현 계약으로 삼는다. 기존 저장소가 있으면 해당 저장소의 기술 스택과 작업 지침을 먼저 확인한다. 신규 프로젝트라면 React + TypeScript + Vite의 클라이언트 전용 구성을 기본 제안으로 사용한다. 별도 서버·DB·LLM API는 필요하지 않다. 의존성 버전은 구현 시 환경에 맞춰 선택하고 잠금 파일을 커밋한다.

데이터 로드/검증, 지표 계산, 필터/정렬, 조건 완화 생성, UI를 분리한다. 점수 계산·필터·대안 생성은 동일한 데이터 정책을 공유하는 순수 함수로 구성한다. 설정 상수로 가중치와 규모 경계를 관리하되 사용자 UI에 가중치 조절 기능을 추가하지 않는다.

### 11.2 작업 순서

1. 저장소 확인 및 실행 골격 구성. 제공 CSV를 원본 그대로 포함하고 기준 해시를 기록한다.
2. 파싱·전처리·규모 경계·신규 분리 구현.
3. 점수·참고 비용·정렬·조건 완화 구현 및 핵심 수용 기준 검증.
4. 입력과 결과 UI, 설명·상태·대안 적용 구현.
5. 대표 시나리오로 브라우저 동작 확인, 빌드 확인, CSV 해시 불변 확인.
6. README.md, flowchart.md, AI_USAGE.md를 실제 구현 및 작업 기록에 맞게 작성하고 PRD와 대조한다.

각 작업 단위로 의미 있는 커밋을 남기며 squash하지 않는다. 테스트 실행 여부와 통과 결과를 실제로 확인한 범위만 기록한다. 위 1~6단계는 1차 구현에서 실제로 수행했으며, 실행·검증 범위는 AI_USAGE.md에 기록되어 있다.

### 11.3 제출 문서의 역할

| 문서 | 담을 내용 |
| --- | --- |
| PRD.md | 본 문서. 문제·목표·정책·제외 범위·완료 기준. 구현 중 변경 시 이유와 함께 갱신 |
| README.md | 실제 설치·실행·빌드·테스트 명령, 추천 설계 요약, 트레이드오프 및 대표 사용 조건 |
| flowchart.md | 실제 입력 검증·파일 오류·신규 분리·예산 필터·빈 결과·대안 적용 분기가 포함된 Mermaid 흐름 |
| AI_USAGE.md | 사용 도구·이유, 실제 프롬프트·컨텍스트, 수정 요청 및 판단 사례, 실제 검증 기록 |

AI_USAGE에는 이미 진행한 ‘참여율 분모 미확인으로 예상 반응수 계산을 배제’, ‘신규의 0원 평균액을 무료로 해석하지 않음’, ‘비용 효율을 기본 점수에 넣지 않고 별도 정렬로 제공’ 등의 결정 과정을 사실대로 기록할 수 있다. AI 제안을 사용자의 독자적 판단으로 바꾸어 쓰거나 실행하지 않은 검증·프롬프트를 만들어내지 않는다.

과제 안내문 원문과 개인 배경 핸드오프를 공개 저장소에 그대로 복사하지 않는다. 제출 대상은 구현 코드와 작성한 문서이며 제공 CSV의 원본 보존 조건을 준수한다. 저장소 공개(Public GitHub 저장소 push)는 사용자 요청에 따라 수행하되, 제출 메일 발송은 별도 요청이 있기 전까지 수행하지 않는다.

### 11.4 완료 정의

브라우저에서 필수 입력→추천→정렬→빈 결과→조건 완화가 동작하고, 신규 후보를 정확하게 구분하며, 주요 수용 기준을 충족해야 한다. 문서의 수식과 구현 결과가 일치하고, 실행 명령이 재현되며, 원본 CSV가 변경되지 않고, 실제 AI 작업 과정과 커밋 이력이 남으면 제출 준비 완료로 본다.

# 흐름도

`src/lib`의 실제 구현(`dataLoad.ts`, `filter.ts`, `scoring.ts`, `search.ts`, `alternatives.ts`, `sort.ts`, `pagination.ts`)과 UI(`App.tsx`, `SearchForm.tsx`, `BudgetInput.tsx`)의 분기를 반영한 Mermaid 흐름입니다. 계산/데이터 처리(1~2절)와 화면 조작(3절)을 분리해, 한 그림에 모든 분기를 넣지 않았습니다.

## 1. 데이터 로드 및 검증

```mermaid
flowchart TD
    A[앱 시작] --> B[public/data/dummy_creators.csv fetch]
    B -->|실패| E1[파일 오류: fetch_failed]
    B -->|성공| C[BOM 제거 후 CSV 파싱]
    C -->|파싱 실패 또는 빈 파일| E2[파일 오류: parse_failed]
    C --> D{필수 헤더 11개 모두 존재?}
    D -->|누락| E3[파일 오류: missing_headers]
    D -->|정상| LOOP_START(["데이터 행마다 아래를 반복<br/>(순서는 실제 코드 순서)"])

    LOOP_START --> G0{creator_id 존재?}
    G0 -->|없음| X1[해당 행 제외 + 사유 기록]
    G0 -->|있음| DUP["id를 등장 목록에 기록.<br/>이미 등장한 id면 duplicateIds에 추가<br/>(여기서 즉시 제외/오류 처리하지 않고 계속 진행)"]
    DUP --> G1{name/category/platform 모두 존재?}
    G1 -->|누락| X1
    G1 -->|존재| H{"필수 숫자 유효?<br/>followers · avg_view_count ·<br/>engagement_rate · campaign_count ·<br/>avg/total budget"}
    H -->|NaN·Infinity·음수·범위 위반| X1
    H -->|정상| I{"campaign_count==0 이고<br/>rating 공란이고<br/>avg/total budget==0?"}
    I -->|예| J[정상 신규 후보<br/>isNew=true]
    I -->|아니오| K{"이력 불일치?<br/>0건인데 평점/가격 존재<br/>또는 양수 건인데 budget==0"}
    K -->|예| X1
    K -->|아니오| L{"campaign_count>0 이고<br/>rating 공란?"}
    L -->|예| M[isRatingUnverified=true<br/>평점 미확인으로 유지]
    L -->|아니오| N[정상 이력 보유 후보]

    J --> LOOP_END
    M --> LOOP_END
    N --> LOOP_END
    X1 --> LOOP_END
    LOOP_END(["모든 행 처리 완료"]) --> O{duplicateIds 1건 이상?}
    O -->|예| E4["파일 오류: duplicate_id<br/>(이 시점까지 분류한 creators는 폐기)"]
    O -->|아니오| P{유효 행 1건 이상?}
    P -->|0건| E5[데이터 오류: no_valid_rows]
    P -->|1건 이상| Q["규모별 기준 집합(baseline) 고정<br/>+ 전체 최대 campaign_count(M) 계산"]
    Q --> R[ready 상태 → 검색 가능]
```

중복 id 검사는 각 행을 처리하는 도중(이름/카테고리 검사보다 먼저) 집합에 기록되지만, 실제 `duplicate_id` 오류는 **모든 행을 다 처리한 뒤 한 번만** 던져집니다. 즉 한 행이라도 id가 중복되면 그 시점까지 분류해 둔 신규/이력 보유 후보 전체가 폐기되고 파일 오류로 처리됩니다.

## 2. 추천 처리 흐름

```mermaid
flowchart TD
    S0["검색 실행 (제출된 예산/카테고리/규모)"] --> S1[카테고리 OR + 규모 필터]
    S1 --> S2[이력 보유 / 신규 후보 분리]
    S2 --> S3[이력 보유 후보에 예산 필터 적용<br/>avg_campaign_budget_krw ≤ 입력 예산]
    S3 --> S4["baseline(규모별 고정 기준 집합)으로<br/>V(조회수)·E(참여율) 백분위 계산"]
    S4 --> S5["협업 점수 C 계산<br/>평점 미확인 후보는 R=50 대체"]
    S5 --> S6["예산 이하 후보: S = 0.60V + 0.25E + 0.15C"]
    S6 --> S8["참고 1천 조회당 비용 계산<br/>budget/view×1000, 조건 미충족 시 null"]
    S8 --> S9{예산 이하 후보 수 > 0?}
    S9 -->|아니오| S10[조건 완화 대안 계산]
    S10 --> S10a["예산 대안: 같은 카테고리·규모 최소 평균 집행액"]
    S10 --> S10b["규모 대안: 같은 카테고리·예산으로<br/>다른 두 규모 각각 계산"]
    S10a --> S11{대안 후보 1명 이상?}
    S10b --> S11
    S11 -->|예| S12[대안 목록 포함]
    S11 -->|아니오| S13[대안 없음]
    S9 -->|예| S14
    S12 --> S14
    S13 --> S14

    S4 --> S7["신규 후보 내부 점수:<br/>(0.60V + 0.25E) / 0.85<br/>(예산 이하 후보 수와 무관하게 항상 계산)"]
    S7 --> S14["기본 정렬(추천순/고정 정렬) 적용 후<br/>{existing, newCandidates, alternatives} 반환"]
```

점수·필터·정렬·대안 계산 로직은 이번 화면 개편에서 변경되지 않았습니다. 이 흐름이 반환하는 `existing`/`newCandidates`/`alternatives`를 화면에서 어떻게 다루는지는 3절을 참고하세요.

## 3. UI 조작 흐름

```mermaid
flowchart TD
    A["예산 입력 조작:<br/>직접 타이핑 / '+10만·100만·1,000만 원' 버튼 / '금액 지우기'"] --> A1["draft.budgetText만 변경<br/>(검색은 실행되지 않음)"]
    A1 --> B{"이전에 제출한 결과가 있고<br/>draft가 그 제출값과 다른가?"}
    B -->|예| B1["'조건이 변경되었습니다.<br/>다시 추천해 주세요' 안내 표시<br/>(이전 결과는 그대로 유지)"]
    B -->|아니오| B2[안내 없음]
    B1 --> C{"'추천 보기' 클릭?"}
    B2 --> C
    C -->|아니오| A
    C -->|예| D[입력 검증]
    D -->|실패| D1[필드별 오류만 표시, 검색 대기]
    D -->|통과| E["검색 실행 → 2절 '추천 처리 흐름' 결과 수신"]
    E --> F["탭 = '예산 기준 후보' · 정렬 = 추천순 ·<br/>두 탭 페이지 = 1로 초기화"]

    F --> G["'예산 기준 후보' 탭"]
    F --> H["'견적 확인 필요' 탭"]

    G --> G1["정렬 옵션 변경 → 이 탭만 재정렬 + 이 탭만 1페이지로 복귀"]
    G --> G2["10명 단위 페이지네이션<br/>이전/다음 이동 (재계산 없음)"]
    G --> G3{후보 0명?}
    G3 -->|예| G4[8절 조건 완화 대안 표시]
    G3 -->|예| G5{견적 확인 후보 1명 이상?}
    G5 -->|예| G6["'견적 확인 필요 후보 N명 보기' 버튼<br/>→ H 탭으로 전환 + H 탭 1페이지로"]
    G4 --> G7["대안 버튼 클릭 → 그 필드 1개만 변경 후 자동 재검색<br/>(F부터 다시 실행)"]

    H --> H1["정렬 옵션 없음 · 고정 내부 정렬만 사용<br/>(내부 점수↓ → 조회수↓ → ID↑)"]
    H --> H2["10명 단위 페이지네이션<br/>이전/다음 이동 (G 탭 페이지와 독립)"]
```

핵심 포인트:

- 예산 입력의 직접 타이핑, 금액 추가(+10만/+100만/+1,000만 원), 금액 지우기는 모두 `draft` 상태만 바꿀 뿐 그 자체로 검색을 실행하지 않습니다. 검색은 오직 ‘추천 보기’ 클릭(C)에서만 실행됩니다.
- 기존 결과가 있는 상태에서 위 세 가지 방법 중 무엇으로 예산을 바꾸든 동일하게 ‘조건이 변경되었습니다’ 안내(B1)가 뜹니다.
- 정렬 컨트롤은 ‘예산 기준 후보’ 탭에만 있고(G1), ‘견적 확인 필요’ 탭(H)은 고정 정렬만 사용합니다.
- 새 검색·조건 완화 적용은 탭·정렬·양쪽 페이지를 모두 초기화하지만(F), 탭을 수동으로 전환하는 것 자체는 상대 탭의 페이지 번호를 건드리지 않습니다.
